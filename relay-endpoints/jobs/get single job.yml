info:
  name: get single job
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/jobs/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
